package PathfindingGUI;

/* all GUI element presets options stored in this class used in GUI element factory to select which preset to create
 new preset objects for GUI */
public enum GUIElementsEnum {
    Window,
    ContentPanel,
    GridPanel,
    SubControlPanel,
    ButtonPresetGray,
    ButtonPresetMedium,
    TimerTextPreset,
    TextPreset,

    SliderPreset,
}
